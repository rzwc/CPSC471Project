@model IEnumerable<RestaurantMS.Models.DrinkItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Drink_capacity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Item_ID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Drink_capacity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Item_ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Drink_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Drink_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Drink_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
