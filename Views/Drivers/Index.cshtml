@model IEnumerable<RestaurantMS.Models.Driver>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Drivers_License_Number)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Account_Id)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Drivers_License_Number)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Account_Id)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Driver_ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Driver_ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Driver_ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
